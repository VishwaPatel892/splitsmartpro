{
  "name": "splitsmart-pro",
  "version": "1.0.0",
  "private": true,
  "description": "SplitSmart Pro – monorepo root",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
